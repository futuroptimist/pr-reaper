{
  "name": "pr-reaper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --test-concurrency=1",
    "test:ci": "npm run build && node --test --test-concurrency=1"
  },
  "dependencies": {
    "@actions/artifact": "^2.1.7",
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5",
    "yaml": "^2.8.1"
  }
}
